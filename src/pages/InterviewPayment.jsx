import { Container, Typography } from '@mui/material'

// 스텁: 실제 화면은 프로토타입(feat/jsc-ui-import)을 참고해 MUI로 단계별 이식한다.
export default function InterviewPayment() {
  return (
    <Container sx={{ py: 4 }}>
      <Typography variant="h4">면접 결제</Typography>
      <Typography color="text.secondary">MUI로 구현 예정</Typography>
    </Container>
  )
}
